
# ETL Process: Import Excel Data into PostgreSQL

## Overview

This project automates the ETL (Extract, Transform, Load) process to import data from an Excel (`.xlsx`) file into a PostgreSQL database. It watches the Excel file for changes and automatically refreshes the database whenever the file is updated.

---

## Prerequisites

- Python 3 installed on your machine.

- PostgreSQL installed and running.

Step 1: Make Sure PostgreSQL Is Running
Before connecting:

Test connection using pgAdmin or Terminal:
psql -U postgres -h localhost -d your_database_name
If this works, you're ready to connect from Excel.

- Required Python packages:
  - pandas
  - sqlalchemy
  - psycopg2-binary
  - openpyxl
  - watchdog


 <!-- Export data from the database  -->

Step-by-Step: Export PostgreSQL Table to .xlsx Using Python
🔹 1. Install Required Python Packages (one-time setup)
In your Terminal:

pip install pandas psycopg2 openpyxl

<!-- pandas: for handling data
psycopg2: for connecting to PostgreSQL
openpyxl: for saving Excel .xlsx files -->

🔹 2. Use This Python Script
Create a file named export_to_excel.py, or run this directly in a Python environment:

🔹 3. Run the Script
In Terminal:
python export_to_excel.py

 4. Open the Excel File
You can open it from the Terminal:
open employee_data.xlsx


<!-- Import data of excel file .xlsx to database into table  -->

create a python file of import_excel_to_postgres.py  <!--  check the code in the file mport_excel_to_postgres.py -->

run the code 

Data imported successfully





